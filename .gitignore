node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# Installed agent skills (local tooling, not app code)
.agents
.claude
.skillkit

# TypeScript incremental build artifact (regenerated on every build)
*.tsbuildinfo

# scratch space for local browser probes
.probe/
